[project]
name = "evaris"
version = "0.1.0"
description = "Trace-based evaluation for AI agents"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
etrace = ["etrace"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/evaris"]
